# 일간 업무일지

## 2026-05-27

### 1. 실습자료 폴더명 인코딩 정리

- 목적: macOS에서 Windows로 넘어오며 깨져 보이는 한글 폴더명을 정상화한다.
- 실행 내용:
  - `실습자료` 내부 모듈 폴더명의 macOS식 분리형 한글 자모를 Windows에서 자연스럽게 보이는 조합형 한글로 정규화했다.
  - 모듈 폴더명 끝에 반복되던 `_실습자료` suffix를 제거했다.
- 산출물:
  - `Module1_GPTs기획`
  - `Module2_데이터분석`
  - `Module3_RAG실무`
  - `Module4_종합실습`
  - `Module5_Codex입문`
  - `Module6_Codex데이터자동화`
  - `Module7_Codex워크플로우`
  - `Module8_Codex종합프로젝트`
- 검증 결과:
  - 남은 분리형 한글 이름: 0개
  - 남은 `_실습자료` suffix: 0개
- 성과 가공 포인트:
  - 운영 환경 전환으로 발생한 파일명 인코딩 문제를 정리해 실습자료 접근성을 개선했다.

### 2. 실습자료 파일 단일 폴더 통합

- 목적: 모듈별로 흩어진 실습 파일을 한곳에 모아 탐색과 활용을 쉽게 만든다.
- 실행 내용:
  - 모든 하위 폴더의 실습 파일을 `실습자료` 루트로 이동했다.
  - 해시가 같은 중복 파일은 하나만 남겼다.
  - 실습 자료가 아닌 `.vscode/settings.json`은 제거했다.
  - 빈 하위 폴더를 삭제했다.
- 산출물:
  - `실습자료` 루트에 실습 파일 16개를 통합 배치했다.
- 검증 결과:
  - 남은 하위 폴더: 0개
  - 중첩된 파일: 0개
  - 최종 파일 수: 16개
- 성과 가공 포인트:
  - 중복 제거와 구조 단순화를 통해 자료 관리 효율을 높였다.

### 3. CSV 시각화 대상 선정

- 목적: 임의의 CSV 중 대시보드 시각화에 가장 적절한 데이터를 고른다.
- 실행 내용:
  - `실습자료` 내 CSV 5개를 확인했다.
  - 행 수, 날짜 컬럼, 범주 컬럼, 수치 컬럼 유무를 비교했다.
  - `자재단가_2022-2024.csv`를 1순위 시각화 대상으로 선정했다.
- 산출물:
  - 추천 대상: `자재단가_2022-2024.csv`
- 검증 결과:
  - 행 수: 302행
  - 기간: 2022-01 ~ 2024-12
  - 주요 컬럼: 날짜, 자재코드, 자재명, 카테고리, 단가_원, 공급업체, 발주수량, 비고
- 성과 가공 포인트:
  - 데이터 구조를 기준으로 시각화 적합성을 판단하고 분석 대상을 선정했다.

### 4. HTML 대시보드 기획

- 목적: 선택한 CSV를 실습용 대시보드로 만들기 위한 화면 구조를 정한다.
- 실행 내용:
  - 단일 페이지 HTML 대시보드 형태를 선택했다.
  - 상단 필터, KPI 카드, 주요 차트, 상세 테이블 구성을 정했다.
- 산출물:
  - 대시보드 구현 계획
- 검증 결과:
  - CSV 구조가 월별 추세, 카테고리별 비교, 공급업체별 발주수량, 자재별 변동폭 시각화에 적합함을 확인했다.
- 성과 가공 포인트:
  - 데이터 분석 요구를 화면 구성과 사용자 탐색 흐름으로 전환했다.

### 5. 자재단가 HTML 대시보드 구현

- 목적: `자재단가_2022-2024.csv`를 기반으로 브라우저에서 바로 열 수 있는 대시보드를 만든다.
- 실행 내용:
  - CSV 302행을 HTML 내부 JavaScript 데이터로 내장했다.
  - 기간, 카테고리, 자재명, 공급업체 필터를 구현했다.
  - KPI 4종과 차트 4종, 상세 테이블을 구현했다.
  - Chart.js CDN을 사용해 차트를 렌더링하도록 구성했다.
- 산출물:
  - `실습자료\자재단가_대시보드.html`
- 검증 결과:
  - HTML 내장 데이터 행 수: 302행
  - 평균 단가: 189,833원
  - 총 발주수량: 79,540
  - 원본 CSV와 HTML 내장 데이터의 핵심 집계가 일치했다.
  - 브라우저 자동화 검증은 로컬 샌드박스 오류로 완료하지 못했다.
- 성과 가공 포인트:
  - 정적 CSV를 인터랙티브 분석 화면으로 전환해 데이터 탐색 가능성을 높였다.

### 6. 대시보드 작업 기록 문서화

- 목적: 대시보드 구현 내용을 나중에 다시 이어볼 수 있게 Markdown으로 남긴다.
- 실행 내용:
  - 대상 데이터, 산출물, 구성, 구현 방식, 검증 결과, 참고 사항을 문서화했다.
- 산출물:
  - `실습자료\자재단가_대시보드_작업기록.md`
- 검증 결과:
  - Markdown 파일 생성 완료
- 성과 가공 포인트:
  - 분석 산출물의 맥락과 재사용 조건을 문서화해 추적 가능성을 확보했다.

### 7. 업무일지 및 워크플로우 규칙 수립

- 목적: 앞으로의 작업을 일간 업무일지와 성과 시각화 자료로 가공할 수 있게 기록 체계를 만든다.
- 실행 내용:
  - 프로젝트 워크플로우 문서를 생성했다.
  - 일간 업무일지 문서를 생성하고 오늘까지의 작업을 항목별로 정리했다.
  - 일반 대화 문장 끝에 이모지 3개를 붙이는 규칙을 추가했다.
- 산출물:
  - `WORKFLOW.md`
  - `일간_업무일지.md`
- 검증 결과:
  - 현재 작업 흐름이 날짜별, 작업별, 산출물별로 기록되었다.
- 성과 가공 포인트:
  - 작업 기록을 성과 요약, 업무 리포트, 시각화 데이터로 재가공할 수 있는 기반을 만들었다.
